# -*- makefile -*-
# Python 3.13+
# 3.12 起 configure 会自动生成 Modules/Setup.stdlib，其中包含所有标准模块的
# 源文件路径和编译参数。只需在此声明 *static*，所有模块将被静态链入 python
# 可执行文件，无需手动列出（手动列出反而会因源文件路径变更或重复定义出错）。
#
# 3.13 的主要变化（PEP 594）：
#   - 删除：audioop ossaudiodev spwd crypt nis aifc chunk imghdr
#           mailcap nntplib pipes sndhdr sunau telnetlib uu xdrlib
#   - _md5/_sha1/_sha2/_sha3/_blake2 改用 HACL* 实现，需要额外的
#     _hacl/Hacl_Hash_*.c 源文件，手写极易遗漏导致 undefined reference
*static*
