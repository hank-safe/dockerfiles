# -*- makefile -*-
# Python 3.14+
# 3.12 起 configure 会自动生成 Modules/Setup.stdlib，其中包含所有标准模块的
# 源文件路径和编译参数。只需在此声明 *static*，所有模块将被静态链入 python
# 可执行文件，无需手动列出（手动列出反而会因源文件路径变更或重复定义出错）。
#
# 3.14 的主要变化（在 3.13 基础上）：
#   - 删除：asynchat asyncore smtpd lib2to3
#   - _ctypes 源码结构重构
#   - HACL* 新增更多 SIMD 变体文件
#   - 新增：_zstd（需要 apt install libzstd-dev）、_remote_debugging
#
# 如需 _zstd 模块，在 Dockerfile 的 apt install 中加入 libzstd-dev 即可，
# configure 会自动检测并加入 Setup.stdlib。
*static*

