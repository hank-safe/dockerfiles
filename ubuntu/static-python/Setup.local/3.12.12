# -*- makefile -*-
# Python 3.12+
# 3.12 起 configure 会自动生成 Modules/Setup.stdlib，其中包含所有标准模块的
# 源文件路径和编译参数。只需在此声明 *static*，所有模块将被静态链入 python
# 可执行文件，无需手动列出（手动列出反而会因源文件路径变更或重复定义出错）。
#
# 3.12 的主要变化：
#   - _sha256/_sha512 合并为 _sha2（sha2module.c）
#   - 大部分模块改由 Setup.stdlib 自动管理
*static*
